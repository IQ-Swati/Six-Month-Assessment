Lab: Configure a security group in AWS that only allows SSH and HTTP access. Provide the security group ID and its rules.

I have configure a security group for VPC in AWS that allows SSH and HTTP access. PDF file is attached in the repository itself where i have added all the snapshots of the practicals.
